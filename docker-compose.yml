services:
  drive-to-soundcloud:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
    volumes:
      - ./.env:/app/.env:ro
      - railway_data:/data

volumes:
  railway_data:
